# Neon / Vercel Postgres connection string (pooled URL preferred).
# Vercel Storage → Postgres / Neon sets POSTGRES_URL automatically.
POSTGRES_URL=

# Neon often also exposes DATABASE_URL. Either variable works.
DATABASE_URL=
